

Run the metif.sh file using the following command:
```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/Kikkerslijm410/MeTify/7-add-proxmox-support/ct/metify.sh)"
```

That will in turn run the metify-install.sh file. However in the current state it wont work because of the fact that it is not hosted within the ProxmoxVE(D) framework.
This will probably be added later, however the project is not fully finished and ready for wider deployment.