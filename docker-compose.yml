services:
  spotdl-app:
    build: .
    container_name: spotdl-dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/downloads
    environment:
      - DOWNLOAD_DIR=/downloads
    restart: unless-stopped
